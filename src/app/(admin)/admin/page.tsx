"use client";

const AdminPage = () => {
  return (
    <span>
      <h1>Admin Page</h1>
      {/* <div>
        {response.data?.data?.notes.map((item, index) => (
          <div key={index}>{item.note}</div>
        ))}
      </div> */}
    </span>
  );
};
export default AdminPage;
